import express from 'express'

const router = express.Router()

// Stub router for local development
// In production/web mode, replace this with actual Stripe webhook handlers

export default router
