# Custom Themes

This directory stores custom chat theme JSON files.

- Files in this folder are packaged with the app as bundled theme resources.
- At runtime, bundled themes can be copied into the managed user theme directory.
- Generated or uploaded themes should use the `theme_manager` built-in tool or the app UI to save valid JSON theme files here.
